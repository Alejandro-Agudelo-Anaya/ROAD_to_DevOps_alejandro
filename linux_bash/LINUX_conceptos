aqui podras encontar varios scripts, donde podras ver la fundamentacion que tengo con el sistema operativo y la bash.
scripts llamado linux-monotoring como su nombre lo dice es un scripts que monitorea la ram y memoria del pc y envia una alerta cuando pase unos umbrales dados, este script se puede programar con un cronjob.

como podras ver tengo conocimientos en linux y la bash
se conceptos claves como 

file system linux comando basicos y complejo para navegar y manipular como cd,ls, mv, cp, redirecciones y tuberias... ect
permisos como chmod donde se cambia los permisos de archivos y chwon donde se cambia el propietario y los grupos
en cuanto a procesos conozco los comandos ps, top, htop, kill donde podemos cobntrolar los procesos y servicios que tiene el pc, como saber su PID, eliminarlos o pausarlos, ponerlos en segundo plano etc.
los servicios (daemons) los cuales inician al comienzo de arrancar el sistema operativo los se utilizar comandos como start... ect
los logs son donde se ven los "informes o mensajes" que nos da el sistema operativos, procesos y servicios. ahi se puede consultar cuando queramos ver una eventualidad (/var/log)  y bash basico como condicionales, ciclos, funciones, etc.  
